import { InfoPage } from '../types';
import en from './returns_en.md';

const returns: InfoPage = {
  // t('pages.info.returns.title')
  title: 'pages.info.returns.title',
  updated: new Date(2021, 4, 16),
  translations: {
    en,
  },
};

export default returns;
