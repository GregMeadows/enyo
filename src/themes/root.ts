import { ThemeOptions } from '@material-ui/core/styles';

const root: ThemeOptions = {
  overrides: {
    MuiTypography: {
      body1: {
        marginTop: '1rem',
      },
    },
  },
};

export default root;
